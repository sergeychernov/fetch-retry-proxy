{
  "name": "fetch-retry-proxy",
  "version": "1.0.1",
  "description": "Fetch with resilience through multiple proxy agents",
  "main": "dist/fetch-retry-proxy.js",
  "types": "dist/fetch-retry-proxy.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.2",
    "typescript": "^4.0.0"
  },
  "keywords": [
    "fetch",
    "resilient",
    "retry",
    "proxy",
    "http"
  ],
  "author": "",
  "license": "MIT"
}
